import { ReactNode } from "react";

export interface ILines {
  fullName: string;
  age: string;
  code: string;
  portfolio: ReactNode;
  telegram: ReactNode;
  github: ReactNode;
  email: ReactNode;
}
